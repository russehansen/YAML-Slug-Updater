# YAML-Slug-Updater
A cross-platform utility for bulk-updating Quickbase Pipeline YAML files when migrating to a new realm. This tool reads exported Pipeline YAML, applies slug mappings from a .env file, and outputs updated YAML ready for import. Works on Windows, macOS, and Linux.
